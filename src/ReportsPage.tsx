import * as React from 'react';

import { Box } from '@mui/material';

import Page from './Page';

let ReportsPage: Page = {
  title: 'Bug Reports',
  component: (props) => (
    <p>test</p>
  ),
  showTitle: true
};

export default ReportsPage;