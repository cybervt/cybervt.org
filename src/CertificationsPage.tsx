import * as React from 'react';

import { Box } from '@mui/material';

import Page from './Page';

let CertificationsPage: Page = {
  title: 'Certifications',
  component: (props) => (
    <p>test</p>
  ),
  showTitle: true
};

export default CertificationsPage;